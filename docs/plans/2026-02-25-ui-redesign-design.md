# 設計書: UI/UXリデザイン — Markdown記事ベースへの移行

**作成日:** 2026-02-25
**ステータス:** 承認済み

---

## 1. 背景・課題

### 現状の問題点

- **学習パスタブが無意味**: Topic → Concept → Term の静的ツリー表示は、左サイドバーと情報が重複。インタラクションもない
- **カードサイドバーの価値不足**: 520px を占有して用語の意味を並べているだけ。記事本文内で十分理解できる
- **3カラムで記事が狭い**: メインコンテンツの表示幅が圧迫されている
- **記事間の導線がない**: 関連記事への遷移手段がない
- **全体像が見えにくい**: カテゴリ横断的な情報整理が弱い
- **コンテンツ管理が不自然**: JSON の `article.sections` に記事を書くのは負担

### 方針決定

- 利用者: 自分専用
- 主な使い方: 記事を読む
- 理想像: 技術ブログ的（Zenn/Qiita風）
- コンテンツ管理: Markdownで書きたい

---

## 2. コンテンツ構造

### データモデルの変更

| 項目 | Before | After |
|---|---|---|
| 構造 | Topic / Concept / Term の3層 | カテゴリ（フォルダ）+ 記事（.md）の2層 |
| 保存形式 | `data/*.json` | `content/カテゴリ/*.md` |
| 記事内容 | JSON の `article.sections` | Markdownファイル本文 |
| メタデータ | JSON フィールド | frontmatter (YAML) |
| 記事間リンク | `[[term-id\|表示テキスト]]` (サイドバーハイライト) | `[[ファイル名]]` Wikilink (ページ遷移) |

### ディレクトリ構成

```
content/
├── ai-basics/
│   ├── _category.yml       # { name: "AI基礎", order: 1 }
│   ├── tokens-and-vocabulary.md
│   ├── embeddings.md
│   └── transformer.md
├── ai-development/
│   ├── _category.yml
│   ├── requirements-with-ai.md
│   ├── vscode-continue.md
│   └── ...
└── model-architecture/
    ├── _category.yml
    ├── self-attention.md
    └── ...
```

### Markdown ファイル形式

```markdown
---
title: トークンと語彙
tags: [nlp, 基礎, テキスト処理]
created: 2026-02-25
updated: 2026-02-25
---

記事本文をMarkdownで記述。

## セクション見出し

本文...

## 関連記事
- [[embeddings]] — トークンをベクトルに変換する仕組み
- [[transformer]] — Attention機構でトークン間の関係を学習
```

---

## 3. UIレイアウト

### 2カラムレイアウト

```
┌─────────────────┬──────────────────────────────────────────┐
│  SIDEBAR        │  MAIN CONTENT                            │
│  (280px)        │  (max-width: 800px, centered)            │
│                 │                                          │
│  ┌───────────┐  │  ┌──────────────────────────────────┐    │
│  │ 🔍 検索   │  │  │  記事タイトル                      │    │
│  └───────────┘  │  │  タグ: nlp, 基礎                  │    │
│                 │  │  更新日: 2026-02-25               │    │
│  AI基礎        │  │                                    │    │
│   ├ トークン   │  │  ## セクション1                     │    │
│   ├ 埋め込み   │  │  本文...                           │    │
│   └ Attention  │  │                                    │    │
│                 │  │  ---                               │    │
│  AI開発活用    │  │  関連記事                          │    │
│   ├ 要件定義   │  │  ・埋め込み                        │    │
│   └ Continue   │  │  ・Transformer                     │    │
│                 │  └──────────────────────────────────┘    │
└─────────────────┴──────────────────────────────────────────┘
```

### ナビゲーション要素

1. **左サイドバー**: カテゴリ別の記事一覧。検索ボックスで記事タイトル+本文をフィルタ
2. **記事フッター「関連記事」**: `[[ファイル名]]` Wikilinkから自動生成
3. **タグ**: 記事タイトル下に表示。クリックで同タグの記事一覧にフィルタ
4. **モバイル**: サイドバーはハンバーガーメニューで表示/非表示

### 削除するコンポーネント

- LearningPathTab.vue, RoadmapBranch.vue, RoadmapNode.vue（学習パス）
- TermSidebar.vue, TermCard.vue（カードサイドバー）
- TabNav.vue（タブ切替）
- ArticleView.vue, ArticleSection.vue（ArticlePage.vue に置換）

---

## 4. 技術アーキテクチャ

### Markdown 処理

```
content/*.md → import.meta.glob + ?raw → gray-matter (frontmatter) → marked (HTML) → v-html
```

- **Viteプラグイン不要**: `import.meta.glob('/content/**/*.md', { eager: true, query: '?raw', import: 'default' })` で文字列読み込み
- **ランタイムパース**: gray-matter で frontmatter 分離、marked で HTML 変換
- **Wikilink処理**: marked のカスタムレンダラーで `[[ファイル名]]` → `<a>` タグに変換

### 新規依存パッケージ

| パッケージ | 用途 |
|---|---|
| `gray-matter` | frontmatter パース |
| `marked` | Markdown → HTML 変換 |

### コンポーネント構成

```
App.vue ─────────── ルート: 状態管理、ルーティング
├── Sidebar.vue ──── 左サイドバー（カテゴリ + 記事一覧 + 検索）
└── ArticlePage.vue ─ 記事表示（Markdown HTML + タグ + 関連記事）
```

10コンポーネント → 3コンポーネントに集約。

### データ読み込み

```javascript
// composable: useArticles.js
import matter from 'gray-matter'
import { marked } from 'marked'

const mdModules = import.meta.glob('/content/**/*.md', {
  eager: true,
  query: '?raw',
  import: 'default'
})

// パースして記事オブジェクト配列を生成
const articles = Object.entries(mdModules).map(([path, raw]) => {
  const { data: frontmatter, content } = matter(raw)
  const slug = path.match(/\/([^/]+)\.md$/)[1]
  const category = path.match(/\/content\/([^/]+)\//)[1]
  return {
    slug,
    category,
    ...frontmatter,
    html: marked(content)
  }
})
```

---

## 5. データ移行

既存の `data/*.json` から `content/*.md` への変換スクリプトを作成。

### 移行対象

| ソース | 移行先 |
|---|---|
| `data/foundations.json` の topics | `content/ai-basics/*.md` |
| `data/architectures.json` の topics | `content/model-architecture/*.md` |
| `data/ai-development.json` の topics | `content/ai-development/*.md` |
| 各 topic の `article.sections` | Markdownの見出し + 本文 |
| `[[term-id\|text]]` リンク | `[[ファイル名]]` Wikilink |
| terms の meaning | 記事本文に統合 or 用語集記事として独立 |
